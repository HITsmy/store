<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!--用于对应AddressMapper接口-->
<mapper namespace="com.hit.store.mapper.AddressMapper">

    <resultMap id="addressEntityMap" type="com.hit.store.entity.Address">
        <id column="aid" property="aid"></id>
        <result column="province_name" property="provinceName"></result>
        <result column="province_code" property="provinceCode"></result>
        <result column="city_name" property="cityName"></result>
        <result column="city_code" property="cityCode"></result>
        <result column="area_name" property="areaName"></result>
        <result column="area_code" property="areaCode"></result>
        <result column="created_user" property="createdUser"></result>
        <result column="created_time" property="createdTime"></result>
        <result column="modified_user" property="modifiedUser"></result>
        <result column="modified_time" property="modifiedTime"></result>

    </resultMap>
    <insert id="insert" useGeneratedKeys="true" keyProperty="aid">
        INSERT INTO t_address(uid, name, province_name, province_code, city_name,
        city_code, area_name, area_code, zip,
        address, phone, tel, tag, is_default,
        created_user, created_time, modified_user, modified_time) VALUES(#{uid}, #{name}, #{provinceName}, #{provinceCode}, #{cityName},
        #{cityCode}, #{areaName}, #{areaCode}, #{zip},
        #{address}, #{phone}, #{tel}, #{tag}, #{isDefault},
        #{createdUser}, #{createdTime}, #{modifiedUser}, #{modifiedTime})
    </insert>

    <select id="countByUid">
        SELECT COUNT(*) FROM t_address WHERE uid = #{uid}
    </select>

    <select id="getAddressByUid" resultType="com.hit.store.entity.Address" resultMap="addressEntityMap">
        SELECT * FROM t_address WHERE uid = #{uid}
    </select>

    <select id="getByAid" resultType="com.hit.store.entity.Address" resultMap="addressEntityMap">
        SELECT * FROM t_address WHERE aid = #{aid}
    </select>

    <delete id="deleteByAid">
        DELETE FROM t_address WHERE aid = #{aid}
    </delete>
</mapper>